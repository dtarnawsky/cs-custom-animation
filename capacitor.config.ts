import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.back.anim',
  appName: 'cs-back-anim',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
